"""
    1) size --> it defines the max. of elements that the stack will hold

    2) top --> it refers to the end from where the elements are added or deleted

    3) push --> it adds a new element at the top of the stack

    4) pop --> it removes the top element of the stack

    5) isFull --> to check that a stack is completely full or not

    6) isEmpty --> to check that a stack is empty or not

    7) peek --> to know which element is at top of the stack
"""